@model List<MainBookVM>
@{
    ViewData["Title"] = "Home Page";
}

<partial name="Landing"/>

<partial name="Main" model="@Model"/>

<partial name="FeaturedBooks" model="@Model" />
<partial name="Reviews" />